#include <iostream>
#include <fstream>
#include <cmath>
#include <vector>

using namespace std;


double error(upper, lower)
{
    for (int i = lower; i < upper; i++){

        //calculating absolute error 
        log10delta = log10(abs(u[i] - v[i]))

        //relative error calculations for log10 as a function of x_i
        log10eps = log10(abs((u[i] - v[i])/u[i]))
        return list; 
    }
}

