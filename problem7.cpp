#include <iostream>
#include <fstream>
#include <cmath>
#include <vector>

using namespace std;


double absolute_error(upper, lower)
{
    for (int i = lower; i < upper; i++){

        log10delta = log10(abs(u[i] - v[i]))
        return list; 
    }
}

double relative_error(upper, lower) //relative error calculations for log10 as a function of x_i
{

    for (int i = lower; i < upper; i++){

        log10eps = log10(abs((u[i] - v[i])/u[i]))
        return list; 

    }

}